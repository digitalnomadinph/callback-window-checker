services:
  - type: web
    name: callback-window-checker-api
    runtime: node
    region: singapore          # closest free region to Philippines
    plan: free
    buildCommand: cd backend && npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
